# 依赖 / 缓存
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# 本地证书与密钥（切勿提交）
certs/
*.pem
*.key
*.crt
*.csr
*.srl

# 环境变量
.env

# 编辑器 / 系统
.DS_Store
.idea/
*.log

# 运行时私有数据（含明文密钥 ha_db_password / jwt_secret / llm_api_key，切勿提交）
data/

# 构建产物 / 日志 / 缓存
*.tar.gz
*.bak
output.txt
.pytest_cache/

# 本地 Agent 数据目录（非源码，切勿提交；请勿删除本目录）
.codebuddy/

# 前端依赖
node_modules/

# 真实 OpenSHS 数据集（89 MB，外部下载，勿提交；复现见 docs/OpenSHS基准报告.md）
# 注意：上面的 data/ 规则已忽略整个 benchmarks/data/，其中 9 MB 的合成样本
# openshs_sample.csv 同样不入库 —— 它由 benchmarks/gen_sample.py 确定性生成，
# 单测会在样本缺失时自动重建（tests/test_openshs_bench.py 的 ensure_sample）。
benchmarks/data/openshs_real*.csv
benchmarks/data/_chunk_*.csv
