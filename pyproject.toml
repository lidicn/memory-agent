[project]
name = "memory-worker"
version = "0.1.0"
description = "忆家管家 - 家庭行为记忆中枢 MCP服务"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "uvicorn>=0.30",
    "starlette>=0.37",
    "python-dotenv>=1.0",
    "chromadb>=0.4.0",
    "redis>=5.0",
    "httpx>=0.27.0",
    "bcrypt>=4.0",
    "python-jose[cryptography]>=3.3.0",
    "itsdangerous>=2.0",
    "mcp>=2.0.0",
    "pymysql>=1.1",
]

[project.scripts]
memory-worker = "memory_worker.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/memory_worker"]
